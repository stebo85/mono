{
  "name": "medgfx-web",
  "projectType": "application",
  "tags": ["type:app", "lang:typescript"],
  "targets": {
    "dev": {
      "command": "bunx --bun vite",
      "options": { "cwd": "{projectRoot}" }
    },
    "build": {
      "command": "bunx --bun vite build",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["^build"],
      "inputs": [
        "typescript",
        "^typescript",
        "{projectRoot}/vite.config.ts",
        "{projectRoot}/src/**/*",
        "{projectRoot}/index.html",
        "{projectRoot}/quicklook.html"
      ],
      "outputs": ["{projectRoot}/dist"]
    },
    "e2e": {
      "command": "bun tests/preview-regression.mjs",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["build"],
      "inputs": ["typescript", "{projectRoot}/tests/**/*"]
    },
    "typecheck": {
      "command": "bunx tsc --noEmit",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["^build"],
      "inputs": ["typescript", "^typescript"]
    },
    "lint": {
      "command": "bunx biome check {projectRoot}",
      "inputs": ["typescript"]
    },
    "format": {
      "command": "bunx biome format {projectRoot} --write",
      "inputs": ["typescript"]
    }
  }
}
