{
  "name": "uikit",
  "projectType": "library",
  "tags": ["type:lib", "lang:typescript"],
  "targets": {
    "dev": {
      "command": "vite",
      "options": { "cwd": "{projectRoot}" }
    },
    "build": {
      "command": "bunx --bun vite build",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["^build"],
      "inputs": [
        "typescript",
        "{projectRoot}/vite.config.ts",
        "{projectRoot}/src/**/*"
      ],
      "outputs": ["{projectRoot}/dist"]
    },
    "typecheck": {
      "command": "bunx tsc --noEmit",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["^build"],
      "inputs": ["typescript"]
    },
    "lint": {
      "command": "bunx biome check {projectRoot}",
      "inputs": ["typescript"]
    },
    "format": {
      "command": "bunx biome format {projectRoot} --write",
      "inputs": ["typescript"]
    },
    "test": {
      "command": "bun test",
      "options": { "cwd": "{projectRoot}" },
      "inputs": ["typescript", "{projectRoot}/src/**/*"]
    }
  }
}
